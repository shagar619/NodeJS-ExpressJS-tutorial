// const { add } = require('./utils/add');
// const { subtract: sub } = require('./utils/subtract');

const { add, subtract: sub } = require('./utils');


console.log(add(1,2));
console.log(sub(1,2));